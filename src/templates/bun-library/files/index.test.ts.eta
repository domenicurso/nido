import { describe, expect, test } from "bun:test";

import { greet } from "./index";

describe("greet", () => {
  test("returns a default greeting", () => {
    expect(greet("Nido")).toBe("Hello, Nido!");
  });
});
